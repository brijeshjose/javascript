//declare a variable to store the price of an item and a variable to store the quantity  purchased.
//calculate the total cost and print it
  var price=108;
  var quantity=5;
   totalcost=price*quantity;
  console.log(`price of the item is ${totalcost}`);