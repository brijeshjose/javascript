//Create a program that prints all even numbers from a given positive
//integer n down to 2 in reverse order using a loop
var num=15;
for(num;num>=1;num--)
{
    if(num%2==0){
        console.log(num);
    }
}